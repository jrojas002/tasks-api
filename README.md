# tasks-api
REST API de gestión de tareas multi-usuario desarrollada con Java 21 y Spring Boot. Permite crear, actualizar, eliminar y cambiar el estado de tareas. Construida con arquitectura en capas (Controller → Service → Repository), JPA/Hibernate y PostgreSQL.
